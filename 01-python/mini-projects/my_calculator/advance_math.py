import math

def power(base,exponent):
    """this will calculate the power"""
    return base**exponent

def square_root(number):
    if number < 0 :
        return "can't do this ops "
    return math.sqrt(number)
